# 1. Save the integrated files
wget integrated_requirements.txt
wget integrated_setup.sh
chmod +x integrated_setup.sh

# 2. Run setup (optionally with cluster config)
./integrated_setup.sh

# For cluster setup:
export IS_CLUSTER=true
export MASTER_IP="10.65.0.2"
export SLAVE_IP="10.65.0.3"
./integrated_setup.sh

# 3. Activate environment and test
source ~/ml_env/bin/activate
python ~/test_ml_setup.py

# 4. Run training
cd ~/ml_reasoning/sft && ./launch_sft.sh    # For SFT
cd ~/ml_reasoning/grpo && ./launch_grpo.sh  # For GRPO
